# Testing notes

Recommended next step:

- add direct-mode tests for `file_dispute`
- add respondent authorization tests
- mock `gl.nondet.exec_prompt` for `analyze_case`
- add state transition tests

